# Copy to .env and set your deployed backend URL.
# Local dev: use .env.local with VITE_API_URL= (empty) so Vite proxies to :5000

# Backend on Vercel (no trailing slash)
VITE_API_URL=https://hrms-backend.vercel.app
